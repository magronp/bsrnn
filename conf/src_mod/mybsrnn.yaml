name: mybsrnn
target: vocals
sample_rate: ${sample_rate}
n_fft: ${n_fft}
n_hop: ${n_hop}
out_dir: ${out_dir}
# Model moyen
feature_dim: 64
num_repeat: 8
# Model gros
#feature_dim: 128
#num_repeat: 12
grad_clip_norm: 5