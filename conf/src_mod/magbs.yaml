name: magbs
target: vocals
sample_rate: ${sample_rate}
n_fft: ${n_fft}
n_hop: ${n_hop}
out_dir: ${out_dir}

# Model
feature_dim: 64
num_repeat: 8
nb_channels: 2
time_layer: gru
band_layer: gru
n_att_head: 0
attn_enc_dim: 20
grad_clip_norm: 5