name: bsrnn
target: vocals
sample_rate: ${sample_rate}
n_fft: ${n_fft}
n_hop: ${n_hop}
out_dir: ${out_dir}

# Model parameters
feature_dim: 64
num_repeat: 8
# The full model (as in the original paper) is as below
#feature_dim: 128
#num_repeat: 12
