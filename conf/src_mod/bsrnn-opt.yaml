name: bsrnn
name_out_dir: bsrnn-opt
name_tblog_dir: bsrnn

target: vocals
sample_rate: ${sample_rate}
n_fft: ${n_fft}
n_hop: ${n_hop}

# Model parameters
feature_dim: 128
num_repeat: 12
time_layer: lstm
band_layer: lstm
n_att_head: 2
attn_enc_dim: 16